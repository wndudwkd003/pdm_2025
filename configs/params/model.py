from enum import Enum

class ModelType(Enum):
    TABNET = "tabnet"
    TABNET_PYTABULAR = "tabnet_pytorch_tabular"
    MULTIMODAL_TABNET = "multimodal_tabnet"
    XGBOOST = "xgboost"
    NODE = "node"
    BASIC_TABULAR = "basic_tabular"
    TABM = "TabM"
    MYMODEL = "mymodel"
    MYMODEL2 = "mymodel2"
